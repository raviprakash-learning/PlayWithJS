/**
 * BASIC DATA TYPES - 2 types
 *  -> Primitive
 *  -> Non-primitive 
 */


// 1. Primitive Data types - 7 types

var name = 'himanshu khosla';                   // 1. String
var number = 200;                               // 2. Number
var twoPhontNumbers = 90071992512474099822n ;   // 3. BigInt
var truth = false;                              // 4. Boolean
var dob;                                        // 5. undefined
var date = null;                                // 6. null
var place = Symbol('new delhi');                // 7. Symbol





// 2. Non-Primitive Data types - n types (will keep on increasing)

var student = {                                  // 1. Object
    firstName: 'ram',
    lastName: null,
    class: 10
};
var gender = ['male','female','trans','others']; // 2. Arrays
var map = new Set(["a","b","c"]);                // 3. Sets